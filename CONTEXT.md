# Context: Agent System Data Access Tools

> **维护约定**：新增的领域概念必须同步加入本文件的 Glossary；被否决的设计分叉放入 Resolved Decisions，避免未来重提。

## Glossary

| Term | Definition |
|------|-----------|
| **MemoryEntry** | 召回结果的通用货币数据类：`source / content / ts / metadata / score`。`source` 取值 `"working.private" / "working.shared" / "longterm"`。`WorkingMemory.recall` 与 `LongTermMemory.search` 返回同构数据，facade 才能去重排序 |
| **ConversationLog** | user 级的对话历史 seam。接口：`append(entries, keep_turns) / get(max_turns) / clear()`。与 agent 无关，因为对话属于 user。实现 `RedisConversationLog`，可选注入 `Compressor` 协议做自动压缩 |
| **Compressor** | 协议：`async def compress(old_turns) → str`。生产实现 `LLMCompressor` 包 `LLMClient`，测试用 `NoopCompressor`。把"LLM 压缩对话"这个跨层依赖从 memory 模块里抽出来 |
| **WorkingMemory** | per-(user, agent) 的短期工作记忆 seam。接口：`remember(content, scope, memory_type, importance) / recall(query, top_k)`。`scope` 三态：`"private" / "shared" / "both"`，默认 `"both"`（与旧 `write_l2(shared=True)` 双写语义一致） |
| **LongTermMemory** | per-(user, agent) 的长期知识 seam。接口：`store(content, metadata, memory_type, importance) / search(query, top_k)`。实现 `PgvectorLongTerm`（依赖 `AgentMemory` 模型） |
| **MemoryFacade** | 组合 `ConversationLog + WorkingMemory + LongTermMemory` 的薄层。给 supervisor 用。提供 `build_context(query)`（recall+search 去重截断）、`remember(content, persist_to, ...)`（按 `persist_to ∈ {working, longterm, both}` 写对应层）、以及 `append_conv / get_conv / clear_conv` 透传 |
| **MemoryManager** | 保留名字的工厂（向后兼容入口）。构造参数 `(agent_type, user_id, agent_name)`，返回的对象实质是 `MemoryFacade`，通过 `__getattr__` 转发旧方法名（`retrieve → build_context`、`write_l2 → remember(persist_to="working")` 等），调用方零改动 |
| **内部数据工具** | Agent 查询系统内部业务数据的 BaseTool，遵循 user_id 自动过滤和敏感字段脱敏原则 |
| **user_id 自动过滤** | 工具层从执行上下文自动获取 user_id，查询时过滤为 `request.user` 或 `tracker_context.user_id`，Agent 无需显式传递 |
| **敏感字段脱敏** | 交易所 API Key/Secret 加密存储，工具返回时只暴露 label/exchange/is_active，不返回明文密钥 |
| **notify_user** | Agent 主动通知用户的工具，从当前执行上下文自动获取 user_id |
| **通知渠道** | 通过 channel_resolver 解析用户活跃的 Telegram 或 Lark 渠道 |
| **通知消息** | 统一格式的普通文本消息，不做 urgency 分级 |
| **告警** | 把一个需要人看见的事实**投递给一个具体的人**的动作。业务动作失败投给受影响用户；机制自身健康投给全体 `is_active` 用户。它既不是一条日志，也不是一个新系统 |
| **上下文 user_id** | 从 AgentMessage.user_id 或 tracker_context 中获取，Agent 不需要显式传递 |
| **水平关键价位** | 由一组 K 线的局部高点或局部低点聚类得到的近似水平价格区域。它不是永久的“支撑”或“压力”；当给定当前价时，低于当前价的水平关键价位可称为支撑位，高于当前价的水平关键价位可称为压力位 |
| **触碰** | 一段 K 线价格进入某个水平关键价位动态容差区间的事件。连续多根 K 线命中同一容差区间属于同一个触碰簇，只计为一次触碰 |
| **触碰簇** | 多根相邻或间隔不足最小间隔的 K 线连续命中同一水平关键价位容差区间形成的集合。触碰簇用于避免把横盘贴线误算为多次有效触碰 |
| **箱体**（Box Range） | 价格在指定时间区间内反复触及上下边界所形成的震荡区域；边界由 ≥2 个局部高/低 pivot 聚合而成且被 K 线触碰 ≥2 次 |
| **上下边界聚合**（Boundary Clustering） | 将窗口内多个局部高/低 pivot 通过容差带聚类形成单一上/下边界价格的过程；算法复用 `horizontal_key_levels` 的 cluster 逻辑 |
| **两次验证**（Dual Verification） | 箱体成立的必要条件——上下边界同时满足 `pivot_count ≥ min_pivots` 且 `touches ≥ min_touches`；单一条件成立不足以认定箱体 |
| **箱体判定**（Box Range Detection） | 给定 K 线、宽度阈值与验证门槛，判断市场是否处于箱体震荡并返回上下边界的处理 |
| **行情阶段**（Market Regime） | 全市场级别的日频状态判定，取值固定四档枚举：上行趋势 / 下行趋势 / 箱体震荡 / 高波动。以大盘（BTC）为单一判定源，不是逐品种判定；品种差异只作为风险叠加维度 |
| **资讯判定**（News Verdict） | 每日行情资讯对行情阶段判定的影响通道。**它参与判定，不是解释性旁注**——量化指标与资讯判定并列为判定的两个输入。与量化指标的关键差别是它的输入不可重放（资讯随时间消失、LLM 输出不可复现），因此它必须落库为可回放的产物（结论 + 引用的资讯条目），否则判定不可归因 |
| **策略-阶段适用性**（Strategy-Regime Fitness） | 由策略历史回测的逐笔交易按行情阶段切片聚合得出的结论，回答「该策略是否适合在该阶段运行」。四态：适用 / 不适用 / 中性（证据不足）/ 未知（无历史数据） |
| **基础阶段 / 抬升标志 / 生效阶段** | 行情阶段判定的三个值，构成两根轴。**基础阶段**：量化指标判出的四档；**抬升标志**：资讯判定是否要求进入风险状态，单向、只能更保守；**生效阶段**：基础阶段被抬升后的值，是唯一产生 gate 动作的那个。三值必须同时留存，否则「是量化判出来的还是资讯抬上来的」事后不可回答 |
| **证据门槛**（Evidence Threshold） | 判定「适用 / 不适用」所需的最小样本：单个「策略 × 阶段」格内 ≥30 笔交易且覆盖 ≥3 个月。不满足者一律记为「中性」，中性不构成停用理由 |
| **重大事件**（Scheduled Macro Event） | 时间可提前确定的市场事件（FOMC / CPI / 非农 / 减半 / 大额代币解锁等），由人工维护入库。第一版只支持日历型，不含突发型 |
| **候选事件**（Candidate Event） | 由资讯判定或 Agent 建议提出、**尚未入库**的事件：有名称、推测时间、来源资讯条目与提出日期，**没有窗口、不产生任何熔断**。唯一出路是经人工确认后转为「重大事件」，或带 14 天失效期被丢弃 |
| **事件熔断窗口**（Event Halt Window） | 重大事件前 2 小时至事件后 1 小时的时间窗，窗内停止开新仓并减仓至阈值 |
| **策略启停强度**（Halt Intensity） | 停止策略的两档强度：「停开新仓」（保留现有持仓）与「停开新仓 + 减仓至阈值」。行情阶段切换只用前者，事件熔断用后者；强制清仓不属于任何档位 |
| **策略原型**（Strategy Archetype） | 策略的粗分类，取值：趋势跟随 / 均值回归 / 突破 / 动量 / 套利。来源是策略 `description` 的「策略类型：」字段，但该字段为自由文本（可能多值、可能含非枚举词），需归一化 + 人工覆盖表 |
| **Shadow 模式** | 机制的一种运行状态：判定照跑、建议照发、**机制自己不施加任何市场动作**。用于在无法做对照实验的实盘环境里验证判定质量与触发频率。**「不施加动作」不是「拦住系统里所有订单」**——它描述的是「机制有没有在动」，不是「系统接不接受订单」；Shadow 期里用户照常在 paper 上跑策略验收（见 Resolved Decisions 的澄清） |
| **机制自熔断**（Meta-Breaker） | 机制自身的失效保护：触发频率持续过高、或误停造成损失超阈值时，自动退回 Shadow 模式并告警。防止机制变成一个没人敢关、又在悄悄改变行为的东西 |
| **停新开仓与放行平仓**（Halt New Entries） | gate 的精确语义：只拦开仓意图的订单，一切平仓单放行。重启后照常启动 runner 但拦掉开仓——因为「不许开新仓」不等于「放弃已有仓位」 |
| **平仓放行标记**（Close Bypass） | 承载「平仓单不受风控拦截」的信号标志。其代码语义是「**免风控**」而非「平仓」：带此标志的订单跳过全部风控检查，包括未来新增的检查。与本机制想要的「放行平仓」结果一致，但标志名表达业务意图、实现做的是权限豁免，两者不可互相推导——任何新增的风控检查都必须确认自己是要被这个标志放行、还是要在它之前 |
| **执行**（Execute） | 本系统里的歧义词：统一执行器的「执行」= 风控校验 + 落单 + **发往交易所**；信号监控的「执行」= 只写一条订单记录、**没有任何下游消费者**。讨论「谁在执行」时必须指明是哪一种，且不可把后者读成「一种只落单的执行方式」——它不是一种执行，是一次不生效的写入（见 Resolved Decisions 中的缺陷记录） |
| **模拟模式**（Paper Mode） | 会话上的一个标记位（`LiveSession.mode`），表达用户对这条会话的定位是「演练」还是「实盘」。**它不决定订单发往哪里**——订单落点由绑定的 `ExchangeAccount.testnet` 决定。两者是不同维度：一个是用户意图，一个是账户属性 |
| **减仓**（Reduce Position） | 在不改变持仓方向的前提下降低持仓规模的动作。与「平仓」是两件事（平仓可到零并可反手，减仓必须到不了零以下）。自动减仓必须由交易所侧强制只减不增，仅靠本地记账的规模推断会反向开仓 |
| **只减不增**（Reduce-Only） | 交易所侧的订单约束：该单只能降低持仓规模，若会导致规模增加则被拒绝。是「减仓」这个动作在交易所层面成立的前提——本地记账与交易所实际持仓不一致时，唯一能保证熔断不会反向加大敞口的手段 |
| **切片**（Regime Slice） | 把一次回测的逐笔交易按「该笔交易的日期落在哪个行情阶段」分组聚合的处理，产物是「策略 × 阶段」的适用性证据。只对**开仓时刻**归属一次阶段，不按持仓期切分 |
| **停用决策**（Deactivation Decision） | 由行情阶段与适用性结论推出的「停开新仓」指令。自动执行，恢复需人工确认；每条都必须能回答「依据哪几个回测、多少笔交易、什么时间区间」。**高波动档不在此列**——它是保命档，不经过适用性判断 |
| **保命档**（Blanket Halt） | 不查适用性、按当前状态对全体策略一律施加的「停开新仓」：事件熔断与高波动档属于此类。它回答的不是「这个策略适不适合这个阶段」，而是「此刻谁都不该开新仓」，因此它没有回测证据作为依据，也不是「停用决策」
| **恢复确认**（Restore Confirmation） | 用户对一条停用决策的显式放行动作。是停用/恢复不对称里的那一半：停自动、恢复不自动 |
| **滑点**（Slippage） | 一次减仓动作的成交均价相对**动作发起时刻中间价**的偏离，逐品种按成交量加权，只计成交的子单。基线刻意不是标记价——那会把市价单必然要付的点差算成滑点，而点差不是冲击成本 |

## Resolved Decisions

### 实盘策略管理机制（行情阶段 + 事件熔断）

**总原则**

- **代价不对称**：误停赚钱策略 = 机会成本（可逆、可度量）；误跑极端行情 = 可能穿仓（不可逆）。不确定时往保守倒。
- **分层默认**：事件熔断默认保命（自动执行）；行情阶段默认维持现状（证据较强才切换）。
- **LLM 只能让系统更保守，永远不能让它更激进**——用噪声当放松风控的理由必须被结构性排除。
- **机制本体是确定性服务**，可测试、可回放、可归因；LLM 只在「判定当前阶段」与「写解释」两步出场。
- **资讯参与判定，且影响较大**（用户裁定，推翻「资讯只做解释文字」的方案）。这意味着 LLM 从「只写解释」升格为「一个判定输入的生产者」，因此它产出的必须是一个**落库的结构化结论**而不是一段文字：判定出错的归因链要能走到「是哪天的哪条资讯让它改了口」。资讯输入本身不可重放（条目会消失、LLM 输出不可复现），所以可回放性只能由「存下结论与引用」来承担，不能靠「重跑一遍」。**资讯只能把阶段推向更保守方向，不能放松**：它就是 LLM 产出，双向影响意味着可以把「高波动」说成「箱体震荡」——直接与「LLM 只能让系统更保守」冲突，且等于把风控松紧栓在一个不可审计的输入上。单向并不削弱它的影响力：把任一阶段抬成「高波动」已足以停掉全场策略。
- **「告警」= 给一个具体的人的即时消息，不是一条日志，也不是一个新系统**：这份设计里有八处「必须告警 / 绝不静默」（撤单失败、减仓失败、滑点超限暂停、脚本回查失败、日报看门狗、事件库 14 天无新增、自熔断、声明写入失败），它们分两类落点——**业务动作失败**（减仓 / 撤单 / 滑点 / 声明写入）→ 即时消息给**受影响用户**，并进当日日报的相关段；**机制自身健康**（任务没跑、投递没到、事件库衰减、自熔断）→ 任务健康检查 + 日报第④段，**同时发一条即时消息给全体 `is_active` 用户**（自熔断与事件库衰减是系统级事实，不存在「受影响用户」这个更小的集合）。**「走已有的任务健康检查通路、不新建告警系统」指的是不要建第二个投递机制，不是「只写进任务健康检查」**：那个通路只对「任务失败」成立，而减仓失败、撤单失败、滑点超限都不是任务失败，它们是**业务动作失败**，需要一个看得见的人。把「告警」含糊地留在文档里，实现时会有人把它做成 `logger.error`——而「沉默必须能被识别为异常」这一整条主线的全部意义，就是**日志不算被看见**。

**行情阶段判定**

- **机制作用域是系统级，不是每用户级**：判定源只有一个，按用户分叉会得出互相矛盾的结论（「张三的 BTC 在牛市、李四的不是」）。halt 与决策全局唯一，按用户展开仅用于归因与通知。
- **全市场级**（BTC 为单一判定源），非逐品种；品种差异只作为风险叠加维度。**v1 的「品种差异」只在日报里以提示形式存在，不产生任何 gate 动作**：没有任何数据能支持叠加门槛该定在哪，而没有门槛的叠加维度会变成随便停。但它不能删——用户会看到日报说「BTC 上行趋势」而自己持仓的品种正在崩，那时「机制在看」就是假的。等 Shadow 数据积累后再决定要不要给行为。
- **日频，北京时间 08:00 出结论，次日 08:00 生效**：今天出的结论明天才咬人，给出整整一天可见期，异常在生效前就能被拦下。事件熔断不受此限，按窗口即时生效。**「待生效」是查询语义，不是需要被推进的状态机**：判定记录带 `effective_at`，取「`effective_at <= 现在` 的最新一条」即为当前生效态。不做「次日再跑一个任务把结论落成生效态」——那会凭空多一个定时任务和一个失败点，那个任务挂了结论就永远不生效且无人知道。**调度用 beat 的 crontab，且业务时区在 crontab 里显式构造**，不依赖进程 TZ（Django 会覆盖进程时区，项目已因此统一到 `apps/common/time_utils.py`）。
- **最小持续期 5 个自然日**才允许切换（阶段是慢变量）。**唯一的例外是资讯抬升**（见「资讯判定通道」）——它按日生效、不受持续期约束。
- **四档固定枚举，互斥且固定优先级**：高波动 > 下行趋势 > 上行趋势 > 箱体震荡（箱体为兜底）。**量化与资讯是两根轴，不写进同一个字段**：量化产出「基础阶段」，资讯产出「风险抬升」开关，生效阶段 = 基础阶段被抬升后的值。三个值（基础阶段 / 抬升标志 / 生效阶段）同时存在，日报才能回答「是 ATR 分位超了，还是资讯把它抬上来的」——这是「每条停用决策都要能回答依据什么」的前提。抬升轴天然单向，顺带兑现「资讯只能更保守」。
- **资讯判定的产物是可回放的结构化结论**：方向 + 是否抬升 + 强度，**连同引用的资讯条目（标题 + 链接 + 来源 + 发布时间 + 送进 LLM 的那段正文，正文截断上限进统一配置面）**、判定时间，一起落库。只存结论会让「为什么抬升」不可回答；只存标题与链接同样不行——站点会改标题、撤稿、改正文，链接会失效，归因链会断在最后一环，而这一环恰恰是最需要被审视的一环。存 LLM 原始响应则不值得——体积大、含噪声，真正需要的是一个稳定结构而非自由格式 blob。**「可回放」的判据是「能重建 LLM 当时看到的输入」**，不是「能重新访问那个网页」。
- **资讯通道失败时退回纯量化并告警**（抓取失败 / LLM 超时 / 返回不可解析都是常态）：量化判定此时仍是一个完整、可解释、可归因的结论，**不是**「判定失败」——「保持上一有效状态」只适用于连基础阶段都出不来的情况。资讯缺席的方向天然偏激进（少了一个只会往上抬的输入），所以必须在日报里显式写「今日资讯判定缺失」，让它是被看见的降级而不是悄悄发生。**不沿用昨日的资讯结论**——那会让一个已失效的结论继续产生熔断。
- **切片用的是纯量化口径的历史标签，与实时判定（量化+资讯）不是同一个函数**：历史资讯不可重建，所以历史标签只能由量化算法产出。这个不对称必须显式记录，并体现在验证里——成功标准①的一致率比对基准是**量化口径**，资讯抬升的日子单独统计。否则一个「资讯抬升判对了」的日子会被记成「判定与历史不一致」，把机制做对的事算成错。
- **阈值按滚动历史分位数自校准**——币圈波动率水平会整体漂移，固定常量必然周期性失效。
- 指标：ATR% 分位（波动）+ EMA 斜率与间距（趋势）+ 箱体判定（震荡）+ 高低点结构。箱体判定原本跟随策略回测 timeframe，做市场级判定必须固定输入为 BTC 日线并另调参数。
- **判定参数全部是可调配置，不是硬编码常量**：ATR 周期（默认 14 日线）、EMA 周期（默认 20/60）、分位窗口（默认 250 日）、高波动阈值（默认 ATR% 分位 > 80%）、趋势判据（EMA 斜率符号与 (EMA20−EMA60)/ATR 同时过阈值）。Shadow 期唯一要验证的就是判定质量，参数焊死在代码里等于取消了这项验证；调整必须留痕。**所有机制阈值收敛到同一处配置**（判定参数、证据门槛 30 笔、事件窗口 2h/1h 与全局上下限 15 分钟~24 小时、滑点上限 0.5%、自熔断频率 20%、恢复豁免 10 个自然日、Shadow 到期下限 20 个自然日与「事件窗口不足」的兜底上限 60 个自然日、成功标准①的一致率达标阈值、日报投递看门狗时刻、事件库「超过 14 天无新事件入库」告警、资讯采集窗口上下限与预筛条数上限、资讯引用正文截断上限、减仓分片数上限、`query_events` 的查询天数），不散落为各模块常量：它们会被一起调整、一起审计，散开后「为什么这个门槛是 30」将无处可查。**v1 不做 per-strategy 覆盖**——否则第一个问题会变成「为什么这个策略不一样」。**v1 这个配置面是只读的**：改阈值必须改代码发版。改阈值是能引发全系统行为变化的高杠杆动作，而 v1 没有任何权限设施（无角色模型），「改动广播给所有人」只能告知、不能阻止；且调整在 Shadow 期本就不该频繁发生（Q65 要的是「可调整且留痕」，不是天天调）。等有了角色模型再开放写入，届时留痕机制已就位。
- **历史标注从回测窗口起点开始，再往前推 250 天做分位窗口预热**：标注只服务切片，多标的部分没有回测数据消费；更早年份的市场结构差异大，把它们的分位混进来会污染当前标定。
- **底层 BTC 日线落库持久化**，作为历史标注与每日判定的同一份真相；库里只存原始 OHLCV，**不存标签**（标签是判定算法的确定性函数，存了会制造第二个真相）。
- **判定失败/数据缺失时保持上一有效状态 + 告警**，绝不降级为「最保守」——否则任务故障会伪装成风控动作。状态过期超 **3 个自然日**则不再新增停用决策（24/7 市场没有「交易日」这个对象，含糊词必然变成两端实现的分歧）。冷启动默认不执行任何停用。
- **风险抬升一律抬到「高波动」，不沿优先级上移**：资讯判定是一段不可复现的文本推理，没有能力分辨「这条新闻值一档还是两档」；沿优先级上移会让抬升幅度取决于当时恰好处于哪个基础阶段，事后无法解释「为什么同样是利空，上周只降了一档」。语义上资讯不是在判定阶段，而是在**声明进入风险状态**，而「高波动」正是为此准备的档位。

**资讯判定通道**

- **采集窗口 = 「上次判定成功时刻 → 本次判定时刻」**，并设下限与上限（上限默认 96 小时，进统一配置面）。固定 24 小时在失败补跑时会永久漏掉中间那天；增量窗口天然覆盖所有未判定的时间，正常日频运行下恰好等于 24 小时。**上限是必须的**：连续失败多日后补跑，输入量随缺口线性增长，那时最容易在异常状态下产生一次不可控的判定。不做固定 72 小时窗——内容大幅重叠，等于同一条新闻被反复计入。
- **采集后先预筛再送 LLM**（来源白名单 + 关键词 + 去重，压缩到固定条数上限），不做全量投喂、也不让 LLM 自行检索。全量投喂的后果不是贵，而是**判定会随当天新闻条数波动**——清淡日与繁忙日输入规模差一个量级，输出自然漂移，且没有东西能解释这个漂移。LLM 自行检索则不可复现、不可审计，与「引用条目必须落库」直接冲突。**预筛规则本身是阈值**，进统一配置面并留痕。
- **采集入口是白名单源的 feed / 公告页直采，不是检索式**：决定性理由不是技术，而是**「今天 0 条」在两种入口下含义不同**——直采下 0 条 = 今天真的清淡（一个可信的判定输入），某个源抓取失败 = 通道故障（可告警、可重试、日报报缺失，退回纯量化的降级路径正是为它写的）；检索式下这两件事长得一模一样，「检索没返回」会伪装成「今天没事」，降级路径永远不会触发。检索式还会让输入取决于第三方搜索当天的排序与收录，与「引用条目必须落库、判定必须可归因」直接冲突。**可复用的是 `web_search` / `web_fetch` 里的 HTTP、代理、超时与正文抽取实现，不是它们的「检索」语义**（两者是 LLM 按需调用的工具，返回文本进对话、不落库）。起点白名单分三类（交易所公告 / 主流加密媒体 / 宏观类站点）落成配置表，进统一配置面，可直接增删。
- **判定结论用 `LLMClient` 新增的 opt-in JSON 模式取回，默认关**：全仓既有的结构化取法只有 prompt 指示 + 正则提取（`intent_parser.py` 的先例），而这条通道每天只调一次、解析失败的直接后果是「整天没有资讯」——缺资讯的方向天然偏激进，最不该拿正则去赌。**默认关**保证既有调用方行为零变化（它们要的确实是纯文本）。**JSON 模式只保证语法合法、不保证 schema**，所以严格校验仍然要做，失败仍然走「退回纯量化」——它降低失败率，不取消失败路径。
- **走统一 `LLMClient`，链路降级属正常服务，只有 `FALLBACK_MARKER` 才算失败**（触发退回纯量化）。降级链是既有的容错设计，把「主模型超时」等同于「资讯不可用」会让判定频繁降级，而降级有代价（当日判定偏激进 + 日报要占篇幅报缺失）。**前提是 `FALLBACK_MARKER` 必须被显式检测**——Skill 层有先例，新路径要复用，否则降级链的最后一跳会伪装成一条正常结论。不指定单一模型：那会新增一个只在资讯通道生效的模型选择逻辑，且丢掉既有容错。
- **资讯抬升不受「最小持续期 5 个自然日」约束，但仍受「次日 08:00 生效」约束**：最小持续期防的是量化判定来回抖，而资讯抬升的价值在于「信息到达时立刻更保守」，压 5 天等于取消这条通道。它不额外开即时生效的口子——那是事件熔断的位置（有明确时间窗语义），资讯的时效边界没有这么硬。若要求抬升自身维持 5 天，会出现「第 2 天资讯已撤回、抬升却撤不掉」的怪状态。
- **资讯可以「建议」新增事件，人工确认后才入库；资讯不得直接沿用事件的高影响结论**。两者的动作量级根本不同：事件熔断会**减仓**，资讯抬升只**停开新仓**——让资讯借用事件结论，等于让一段不可复现的文本推理越过「自动减仓」的高门槛（它依赖只减不增标志、滑点上限、不自动重试）。反过来，资讯是事件库覆盖率衰减的天然补充信号，人工据此补录正好补上那个缺口。**建议的出口是「候选事件」表，不是任何自动入库**：候选落库（名称 / 推测时间 / 来源资讯条目 / 提出日期），出口是**日报第③段末尾单列一节**与 `query_events`（**必须与已入库事件在结构上区分**——候选没有窗口、不会熔断），确认走 slash 命令。**不单独发即时通知**：那是 LLM 的一段文本推理，不该按「动作已发生」的级别叫醒人，而事件有数天提前量，日报足够。**候选带 14 天失效期**，到期未确认即丢弃并记日志（与「超过 14 天无新事件入库」同节律）；不做自动入库，也不让候选永久挂着——那会变成第二个没人看的待办堆。**Agent 的建议与资讯的建议走同一条出口**：两者都是候选、都由人确认，Agent 那条不因为走了对话就多一分可信度。

**策略-阶段适用性**

- 排序指标：**分段最大回撤 + 分段 Calmar**；不用纯收益（会被单次运气主导）。
- **证据门槛：每格 ≥30 笔交易且覆盖 ≥3 个月**。不足者为「中性」，**中性不构成停用理由**。稀有阶段按该阶段占天数的比例缩放门槛，但必须同时输出置信区间。
- **禁止跨阶段外推**；分段结论与全样本方向矛盾时视为样本噪声。
- 跨阶段交易的赋权**刻意不对称**：PnL 按持仓时长 pro-rata 分摊（求准），笔数按入场段计 1 笔（求保守）。
- 键：**记录到（策略 × 品种 × 阶段），决策按（策略 × 阶段）池化**；池化结论与某品种独立表现明显冲突时转人工复核。样本不足时用「策略原型 × 阶段」池化兜底并标记来源。**原型的归一化走「代码内关键词映射表 + 数据库人工覆盖表」**，不用 LLM 归类——机制本体是确定性服务、LLM 只在「判定当前阶段」与「写解释」两步出场，归类不在那两步里。覆盖表是**数据不是阈值**，所以不进统一配置面，单独落表并留痕。若只做精确匹配（不归一化），池化兜底几乎永远匹配不上，等于这个兜底不存在。
- 切片结论物化到 `BacktestResult.metrics`；「日期 → 阶段」标签表是唯一真相。**不给逐笔交易打阶段字段**——判定算法会随数据增长漂移，冻结的标签必然变错。
- **切片在回测完成时自动计算并写入，同时必须有一个全量重算入口**：判定算法一调整，全部历史标签都会漂移，所有既有切片同时失效——这不是增量更新能覆盖的。只有自动计算而没有重算入口，算法一改就会静默留下一批用旧标签算出的结论，而它们看起来完全正常，这是最难发现的一类错误。**切片计算不放在回测任务的收尾里，而是回测结果落库后投递一个独立的轻量任务**：网格搜索是一个任务里跑上百个组合、硬超时 7200 秒，每个组合收尾同步切片会把「逐笔交易 × 标签」的开销乘上百倍，有打死整个作业的风险，而它死时已经跑了很久。异步化同时买到三样：网格不被拖死、切片失败不反噬回测结果（回测数据是更贵的资产）、失败的格子可单独重试。代价是切片结论晚几秒到几分钟，没有消费者依赖这个延迟。
- **被管策略集合 = 能在策略注册表解析到实现类、且被至少一个活跃实盘会话引用的策略**。`is_active` 不作为集合依据。历史回测自动创建出的幽灵策略行**先清理**：无实现文件且从未被会话/回测引用 → 删除；有历史引用 → 保留但停用。带着幽灵行会让日报变成几十条「未知」。**删除本身必须走带 dry-run 的管理命令**（默认只打印待删清单，`--apply` 才真删），不走数据迁移：迁移会在部署时自动执行、无人复核，而这是不可逆地删掉几十行。**回填 `is_active=True` 那部分可以走迁移**（幂等、安全）。
- **`is_active` 被赋予真语义 = 人工总开关，`False` 即「已退役」**（永不被自动停用，也永不被自动恢复）。这与 gate 的临时停用是两回事，删掉字段只会让下一个人再造一个。**必须同时回填现有真实策略为 `True`**——给一个默认全 `False` 的字段赋予「`False` 即退役」的语义，等于一次性退役全库，这是个只在迁移瞬间成立的陷阱。
- **样本先做厚，再谈机制**：先在 4 年窗口为现有策略各重跑一次长回测（一次性成本约 20 分钟），再切片。稀有阶段样本不足就**诚实显示「中性」**，不为让机制看起来有输出而降低门槛——门槛是「用十几笔交易停掉一个策略」的唯一天然刹车。
- **「高波动」档不做适用性判断，一律停开新仓**（不含减仓）：它是保命档，不是适配档——它问的不是「这个策略适不适合」，而是「此刻谁都不该开新仓」。这一条是必需的，否则资讯抬升到高波动之后**什么都停不掉**：高波动恰恰是样本最稀有的阶段，多数策略在这一格是「中性」，而「中性不构成停用理由」会把资讯通道的全部动作量退化成日报里的一行字。**代价必须写明：证据门槛在高波动档失效**——门槛是防「用十几笔交易停掉一个策略」的刹车，而这个档位的停用本来就不基于证据；**切片对高波动阶段无实际作用**，将来不要以为这里走的是适用性。这也让「停用决策」的定义保持干净：高波动与事件熔断一样是 **halt 的触发源**，不是「依据适用性推出的停用决策」。

**策略启停**

- **行情阶段切换 → 停开新仓，且「停用自动、恢复需人工确认」**（停 = 保守方向自动执行；恢复 = 重新下水需确认）。
- **halt 的停用有两个来源，判定链完全不同，不得混为一谈**：一是**适用性驱动的停用决策**（逐策略、有回测证据、恢复需人工确认）；二是**保命档**（事件熔断与高波动档，对全体一律、无证据依据、按状态成立）。混起来的后果是判定链断掉：日报与告警必须能区分「你这条策略因为在下行趋势里表现不佳被停」和「此刻谁都不该开新仓」，两者恢复方式与责任归属都不同。
- **高波动档的强度是「停开新仓」，不含减仓**（它不是事件熔断）：触发它的是慢变量判定（日频、次日 08:00 生效），而减仓是一次不可逆的规模削减，两者量级不匹配；敞口在极端行情里往下走的决定留给事件熔断与用户自己。
- **保命档的解除是自动的（状态派生），不需要人工确认**：高波动档是「叠加在当前状态上的保命层」，状态过去它就消失，系统**回落到适用性层**——即在箱体震荡里判为不适用的策略仍然停着、且仍需人工恢复。**「叠加层解除」与「停用决策恢复」是两件事，不可混为一谈**：若保命档也需人工点头，资讯抬升这条通道第二天就撤不掉（与「资讯抬升不受最小持续期约束、按日生效」直接矛盾）；且每天要一次人工点头会把恢复确认变成打卡任务——这正是事件库覆盖率衰减之外，同一类疲劳的另一个入口。**解除必须出现**在日报第②段「相对昨日的变化」里，不允许静默恢复。
- **减仓由一次性任务投递触发源，不塞进 halt 判定**：halt 判定被定为「同步、纯状态查询、无任何 I/O」——这对「停开新仓」成立（拦一下，天然幂等），但减仓必须发生**恰好一次**，查询表达不了它。**窗口开启时由一次性任务投递一条「减仓」触发源**，动作侧按 `(交易所账户 × 事件 × 第 N 次 × 品种)` 落幂等记录，重复投递不产生第二次减仓——**幂等键必须含「第 N 次」**，否则同日的多次高影响事件叠加（第二次减到基准 25%）会被幂等键吃掉。**减仓不加自动重试，所以一次性保证只能靠这条幂等记录**；记录写失败等同于「没有减仓」，必须告警。绝不把减仓做成「读到刚进入窗口就顺手触发」的副作用。
- **减仓的作用对象是「账户持仓」，不是「会话」**：全仓**没有任何持仓表**，持仓只存在于交易所账户侧（`adapter.get_positions()` 实时拉）——持仓属于**账户**，不属于会话；而会话是会消失的（`status` 有 `stopped` / `paused` / `error`，且**没有任何代码会在会话停止时关仓**）。把减仓挂在会话上，等于用一个会消失的东西去指代一个不会消失的东西，而它消失的时刻恰恰是熔断最该保护的时刻（用户停会话常常正是因为觉得市况不对）：那笔敞口会被熔断完整放空。所以作用面按「受影响品种」逐个账户持仓，幂等键含**账户**而非会话；同一账户下两个会话跑同一品种时，那是**一次**减仓，不是两次。**减仓结果通知的受众因此独立于窗口通知的裁剪规则**：按「该账户在该品种上有净持仓」判定——**只要减了你的仓，你必然收到那条通知，哪怕你当时没有任何活跃会话**（此决策的背景与代价见 `docs/adr/0003-reduce-targets-account-position-not-session.md`）。窗口开启/结束两条通知的裁剪规则不变（那是「此刻谁该知道要出事」，提醒可以裁掉噪声），而减仓通知是对一次**已发生的不可逆动作**的告知，告知不能裁。
- **池化后的适用性结论必须落库**（独立模型，键为「策略 × 阶段」+ 证据摘要），不每日现算：每条停用决策都要能回答「依据哪几个回测、多少笔交易、什么时间区间」——这是「停用全自动」的代价，审计是硬要求；现算不落库会让「为什么停我」永远答不出来。**池化表的全量重算并入切片重算入口，不另开一条命令**：切片重算 → 全部回测切片落库 → 全量重建池化表（同一任务串行；池化只是聚合，开销远小于切片本身）。**必须先失效后重建**：落地前把当期池化表整体标记为「重算中」，重建完成后原子替换——否则重算跑到一半时，每日判定读到的是新旧混合的表。**每次重算必须落一条记录**（触发人 / 时间 / 涉及其几个回测 / 重算前后的结论差异条数）：它改变的是机制**全部行为依据**，且与回测数据解耦——没人重算时它永远不发生，发生时不看记录也永远不知道。这与「切片重算入口」是同一条理由的第二次落地。**重算不自动恢复任何停用决策，但必须把差异摆到人面前**：重算完成后对所有**生效中的**停用决策重放一遍新表——仍判为不适用/中性的原样保留；**变为适用的标记为「依据已随重算失效」**，并计入当日日报第②段与一次即时告警（重算是人触发的低频动作，告知触发者与告知受影响用户是同一件事）。**不选自动恢复**：「恢复需人工确认」是这份设计的地基之一，重算只是它的一个内部事件，不该成为绕开它的后门。**停用决策记录本身不被覆盖**：它的证据摘要（哪几个回测、多少笔、什么区间）是那条决策**成立时刻**的快照，属于历史；新表结论作为一条独立的**重算差异**记录挂在它旁边——覆盖会让「为什么当初停我」永远答不出来，而不标记会让「依据已失效」静默累积成一条看起来完全正常的停用。
- **恢复确认走 slash 命令**，不用自然语言、不用按钮：恢复罕见但必须可靠，不能依赖 LLM 意图识别，且当前渠道不存在任何按钮基础设施。**注意 Telegram 侧需单独注册**——它的消息过滤器把斜杠消息挡在 Agent 之外，只改 Agent 的命令表在 Telegram 上不生效。**修 Telegram 的斜杠通路是 v1 前置，不是顺手改**：它的消息过滤器把斜杠消息挡在 Agent 之外（`/new`、`/cancel` 长期因此失效），不修则恢复确认在 Telegram 上根本无法触发——而那是「停自动、恢复人工」不对称里唯一的另一半。
- **恢复产生的是一条有时效的人工豁免，不是一次性的开关动作**：否则次日判定仍会说该策略在该阶段不适用，于是又把它停掉——用户确认一次、系统推翻一次，无限循环，每次循环还发一条通知。豁免键为（策略 × 阶段）、带生效期，期内不再被自动停用（判定照跑，日报记「已豁免」）；**阶段离开该阶段时豁免自然失效**，所以它不会变成永久免疫。豁免到期后若判定仍说不适用，会再停一次并明确告知「此前的恢复豁免已失效」。**豁免生效期固定 10 个自然日，与「阶段离开」两条失效条件取先到者**（10 天是可调配置，与判定参数同一配置面）：量级与「日频判定 + 最小持续期 5 个自然日」对齐，最坏情况下「停—恢复—再停」的拉锯被压到每 10 天一次而非每天一次，同时不让一个真不该跑的策略跑满整个阶段。无上限的豁免会变成永久免疫，正是这里要避免的。
- **事件熔断 → 停开新仓 + 减仓至 50%，全自动（含减仓），不等确认**。
- **halt 生效时必须一并撤销市场上未成交的开仓挂单，撤单失败则告警**：停开新仓若不管挂单，这个「停」就是有洞的——极端行情下最危险的恰恰是那批「等回调再买」的挂单，而事件熔断的全部意义就是让敞口在事件前不再增加。撤单失败意味着敞口仍会自动增加，必须升级为告警，不能静默。**不区分触发源**：行情阶段切换与事件熔断都撤，不做「只有事件熔断才撤」的第二套语义（整份设计一路在清除的正是这个，见 ADR 0001）。**枚举来源必须是交易所侧，不是本地 `Order` 表**：适配器目前只有 `cancel_order(exchange_order_id, symbol)`，**全仓没有 `fetch_open_orders`、也没有任何地方用到 `/fapi/v1/openOrders`**，所以「有哪些挂单」根本无处可问；而能充数的本地表恰好是文档反复点名的坏数据（悬挂 `pending` 行、「无交易所 ID 则跳过」、崩溃窗口里 `exchange_order_id` 为空——**而那批行恰恰可能在交易所侧真的开着**）。拿本地表当枚举源会同时产生两个方向相反的错误：**本地有、交易所无** → 撤单报错、被记成「撤单失败」并告警（假告警）；**交易所有、本地无 ID** → 撤不掉，而它正是最危险的「等回调再买」那批。**补 `fetch_open_orders`（`/fapi/v1/openOrders`），本地行只作对账输入、不作撤单依据**；每条待撤单按 `clientOrderId` 与本地对账，对不上的照样撤（交易所是真相）；**「开仓挂单」按「与当前账户持仓方向相反」判定，无持仓时全部挂单都算开仓意图**（一只空仓账户上的买单**就是**开仓单，这一条与减仓的作用对象同为账户持仓维度）；撤单结果与减仓结果**并列在同一条通知**里报出，撤不掉的那批必须点名，不能只报一个「部分失败」。**撤单先于减仓，同一一次性任务内串行、不并发**：先减仓后撤单的话，那张「等回调再买」的单可能在减仓过程中成交，于是**按当时敞口算出的一半，在执行完成时已经不是一半**——而「事后能向用户解释减了多少」是硬要求，这个数会无法解释。**撤单失败不阻断减仓**：减仓是主目的（把敞口降下来），撤单是防它再涨回去；为了等一个撤不掉的单而推迟不可逆的降敞口，是拿主目的去保次目的——而减仓本身有只减不增保护，撤单失败不会让它变得不安全。撤单失败走即时告警，并在**同一条**通知里写清「这批挂单未撤掉，敞口在窗口内仍可能增加」；**窗口结束时仍未撤掉的转成一条独立告警并进日报**——窗口结束不等于它会自己消失。**撤单的枚举整体失败不阻断减仓**：`fetch_open_orders` 自己会失败（超时 / 权限 / 账户不可达），而「不知道有哪些挂单」不改变「敞口现在就必须降下来」这个判断——与「逐单撤单失败不阻断」同一条理由（拿主目的去保次目的）。枚举失败走即时告警（属「机制自身健康」那一类），结果里**必须显式写「本次未能枚举挂单，敞口在窗口内仍可能增加」**，不能省——否则用户会以为「撤单成功」，而实际上根本没查。**「窗口结束时仍未撤掉的」由窗口结束的那次一次性任务重新枚举判定，不用开启时那份清单**：结束时重调 `fetch_open_orders`，按「该账户该品种上仍存在与持仓方向相反的挂单」判——与开启时同一口径，交易所是真相。**不选「把开启时撤单失败的清单持久化到窗口状态行、结束时原样读出」**：那张单可能在窗口内被成交、被用户手工撤掉、或自己过期（`EXPIRED` → `cancelled`），那份快照会说「仍有挂单未撤掉」而敞口早已不存在——**假告警的代价不是噪声，是它会让真告警也被忽略**；且它要求把一份状态跨任务保存到一个可能相隔 24 小时（窗口上限）的时点，多一份会漂移的东西。**这条告警与「悬挂 `pending` 行按 `clientOrderId` 反查」的 beat 兜底扫描是两件事，不得合并**：后者的产物是**本地行的状态**（failed / 未知），它不报「你现在有一个活着的敞口」；前者的产物是**给一个人看的即时消息**，且必须发生在窗口结束那一刻——合并等于让一条按节律跑的扫单任务去承担一个时点精确的动作。
- **「减仓至 50%」= 每个受影响品种各自减半**，不是组合总敞口减半：后者会出现「某品种完全没被碰、另一个被清掉」，事后无法向用户解释。**「每个受影响品种」是账户持仓维度上的品种**（见上条），不是「每个会话的品种」。
- **减仓不设「补回」**：窗口过去只解除开新仓限制，不自动恢复到减仓前规模。事件刚过波动率仍高，自动补回等于重新加大敞口；且「原规模」在减仓那一刻已不存在，补回是一次**新的开仓决策**，不该伪装成恢复。
- **减仓：按风险贡献降序（波动率 × 仓位价值）、市价 + 分片、有限重试（≤2 次）后立即告警，绝不静默。** **「≤2 次重试」是子单级的**：只允许重发同一张子单，且**必须复用同一个 `clientOrderId`**——幂等记录的粒度是整个减仓动作，而分片把它拆成 N 张子单，子单级的重发没有任何幂等键保护；重试时若生成新的 `clientOrderId`，一张其实已成交的子单会被再下一遍，那一格就减了两次（敞口到基准的 25% 而非 50%）。重试**不消耗新的幂等额度**（幂等记录仍按动作粒度落一条）。子单重试耗尽后**不降级为「换一张新单接着减」**，直接按分片失败处理：计入「部分减仓」的结果清单并告警。
- **分片是「同一品种内拆成固定数量的子单」，不做时间摊平**（分片数进统一配置面）：事件熔断要的是「在事件前把敞口降下来」，时间摊平与这个目标直接冲突——摊平意味着事件来临时只减了一半。分片的作用是降低单笔市价冲击，不是择时。**也不做「每个品种一单市价全平一半」**：高波动时单笔市价滑点会直接撞上 0.5% 上限，导致「告警并暂停自动减仓转人工」——即熔断在最需要它的时候自己停下了。
- **分片数是「上限」，且每片的名义价值不得低于交易所最小额，否则自动降片**：分片数是配置值，但**仓位大小是一个它管不了的事实**——适配器只读了 `LOT_SIZE`（stepSize / minQty，`binance.py:326-337`），**没有读 `MIN_NOTIONAL`**，一笔小仓位按固定片数拆开，每片的名义价值会低于交易所最小额，直接拒单。后果不是「少减了一点」：每张失败子单都计入「分片失败」，**滑点与失败率同时被污染**，而这个污染会直接触发「滑点超限则暂停自动减仓转人工」——与上一条同一种失败：熔断在最需要它的时候自己停下了。所以规则是：**降片至每片达标为止；降片后仍不足最小额则整笔一次市价减仓**，并在这条路径上留一条显式记录——**「整笔一次」是唯一一条合法的「分片数不生效」路径，必须写明**，否则实现时会有人为了满足配置值硬拆。**同时给适配器补 `MIN_NOTIONAL` 的读取**：不加的话这条规则在代码里没有落点，「每片是否过小」无处可算，而算错的代价是拒单而不是报错。**不选「把分片数调小」**：那等于用配置去回避一个由仓位大小决定的事实，大仓位会被无谓地少切。分片存在的理由是**降低市价冲击**，不是「必须切成 N 片」。
- **自动减仓的前置条件是「只减不增」标志**：现有订单请求只有 side/quantity/price，持仓规模依赖策略侧乐观记账。按本地记账算出的「一半」在记账失真时会反向开仓，即熔断动作反而加大敞口。必须在订单请求上加该标志、由各交易所适配层翻译成本地约束，**在此之前不得开启自动减仓**。**`clientOrderId` 的接通与它同级、同为硬前置**：没有它，「分片子单可重发」在交易所侧不可去重，而重发是减仓唯一的容错手段。
- **减仓成本上限：单次熔断减仓滑点 > 0.5% 则告警并暂停自动减仓、转人工**，并写入机制自熔断条件。**滑点口径必须一并定死，否则这个阈值无处可算**：**参考价取动作发起时刻的中间价 `(best bid + best ask)/2`**（新补一次只读的 `/fapi/v1/ticker/bookTicker`），**不用 `markPrice`**——那会让市价单必然要付的点差进入滑点：减仓方向恒为「卖多 / 买空」，成交价天然落在中间价的另一侧，于是任何一个点差宽的小币种都会在正常动作里越过 0.5%，把「转人工」触发在一次本来正常的减仓上，与「分片数不足交易所最小额」是同一种失败（熔断在最需要它的时候自己停下）。**逐品种按成交量加权，不做全动作混合、也不取最差一片**：分片与滑点的载体是**品种的流动性**，混合会让 BTC 的干净成交把一个流动性差的品种的 0.8% 稀释掉，而「取最差一片」会让单片噪声决定是否停手。**参考价取在撤单之后、第一片之前**：撤单本身会改变盘口，不该由它决定减仓的成本基线。**只有成交的子单进入计算**，被拒的子单纯计入失败率——它对滑点的污染是**间接的**（降片 → 每片变大 → 下一片成交更差），不是直接计入。**参考价取不到时按「本次不判定滑点」处理并留显式记录，不按 0 处理**：按 0 会让一次判定失败伪装成「成本为零」。**这条的前半（告警 + 暂停自动减仓转人工）是活的，后半（写入机制自熔断条件）在 v1 是休眠的**——v1 只启用频率条款，与「误停损失」同一处：字段留位，不假装生效。
- gate 是**独立模型**（不混入 `LiveSession.status`——那个字段表达用户意图，halt 表达系统施加的状态）；作用域字段表达 **global / symbol:<品种> / strategy:<id> 三档，优先级 global > symbol > strategy**。**第三档是必需的，不是可选的**：停用决策是「某条策略」的问题（与品种无关），事件熔断是「某个品种」的问题（与策略无关）——把后者压进 `strategy:<id>` 列表，就是用一个**随会话启停漂移**的实时交集去表达一个静态事实，而漂移方向恰好是「窗口中间新起的会话不在列表里、从缝里穿过去」——漏拦在极端行情里就是敞口。**两处调用点（统一下单咽喉、信号监控）都必须把订单的 `symbol` 带上**，否则这一档无法求值——**事实核查**：同步侧确实拿得到（`monitor.symbol`，同一 `BASE/QUOTE` 口径，`signal_monitor/engine.py:470` 直接用它建单），但 Q127 收窄三个入口之后那条路径不再能建单，因而这个要求目前只在异步咽喉一处真正落地（见 Q142）。**gate 不是一行布尔，是一组声明，唯一键为（触发源 × 作用域），每行带自己的生效期**；「当前是否拦」= 存在任一命中的生效行。解除（人工恢复或状态派生）**只终结自己那一行**，不动别人。理由：这与 ADR 0001「停止的语义是『谁投的触发源』」是同一句话在存储层的落地——多个源本来就该是多个对象；塌成一个布尔，先解除的那个会把行删掉、顺带解除另一个，那是一次**静默恢复**（Q111 明令禁止）。Q111 的「保命档解除是状态派生的」在这个形状下就是「删掉高波动档那一行」，适用性停用的行不受影响。
- **声明随会话消失而自然失效，且不新增清理任务**：`strategy:<id>` 层的求值**必须先过「该策略仍在被管策略集合内」这一关**——不在集合内则该行不参与「当前是否拦」，但**行本身不删**（它是一条历史，删掉「为什么当初停我」就答不出）。`is_active=False`（人工退役）同样走这条：退役即不再参与求值，记录保留。这与「声明是对象、解除只终结自己那行」不冲突——这里不是谁替我解除，而是**我的作用面已经不存在了**，与「某品种不再被任何会话交易」是同一种自然失效，也是「被管集合 = 仍被至少一个活跃实盘会话引用」那条定义的直接推论。**不选「加一个清理任务扫描失效声明」**：那会引入一个只在边缘情况下才动、失效时无人知道的后台任务（本机制已反复否掉这个形状），而求值期的过滤是幂等的、没有失败模式。
- **gate 必须持久化**（扛过进程重启）；**重启后照常启动 runner 但拦掉一切开仓**，不是不启动（不启动会让存量仓位失去止损保护）。
- 统一下单咽喉：在 `RiskGuard.pre_trade_check` 加第 0 步，三处检查点自动继承；`signal_monitor` 的旁路也必须堵。**这一步必须加在 `user_id` 早退之前**：该函数在 `user_id` 为空时直接返回 `(True, "OK")`，而 halt 是系统级的，不能因为订单没带用户而放行——这是「机制作用域是系统级」的直接推论，不是新裁决。**三处调用点**（`executor.py:226`、`signals.py:117`、`paper_trader.py:63`）都通过 `OrderRequest.symbol` 拿得到品种，格式为 `BASE/QUOTE`（不含结算后缀，转换只发生在适配器 `binance.py:377`）。
- **halt 判定实现为同步的纯状态查询函数；v1 里它只有「统一下单咽喉」一个现役调用点**，信号监控侧是**终局接入点**（为「信号监控改走统一执行器」预留），当前**不可达**。事实核查：`_execute_trade`（`signal_monitor/engine.py:453-486`）走 `Order.objects.create`，从不调 `RiskGuard`（全仓 `grep "_check_risk"` 无结果），而三个入口被收窄之后，同步路径上不再存在任何可达的建单入口。**保留「同步可调用」这个形状**：halt 判定只需读一行状态、没有任何 I/O，做成 async 就只能被一边用、另一边必然走样——而「两套停用逻辑」正是本机制最不该有的东西；且它是终局形状，事件熔断层无 I/O 之后保持同步可调用**成本为零**，删掉反而会让将来接通时被迫改函数签名。**但必须写明「v1 只有一个调用点」**：把「两处各自调用」原样留着，会让下一个实现者去找那个同步调用点、找不到，然后**为了兑现文档去补一个调用点**——在一条已被判定为断路径的代码上补风控检查，是把已清除的东西重新长回来。**事件熔断层的入参是（当前时刻 × 订单 symbol）**，而它仍然无 I/O：窗口开关**由一次性任务在开启与结束时把「事件熔断层」落到状态行**，判定函数照旧只读状态、不读事件表。**不选「把事件表读进进程内存缓存、判定 = 纯函数 f(内存表, 现在)」**：事件会被改期与取消（改期从下一个窗口起生效），内存表一旦与服务端不同步，熔断会在**错误的时刻**生效或失效——而这类错误只在改期那天出现，平时完全看不出来，等于引入第二个真相。**代价必须写明**：该层的存在依赖那个一次性任务真的跑了，它没跑就等于这一层静默消失，所以它必须进「新任务的两条失败可见性路径」（任务健康检查 + 日报第④段）。复用 Q112 已裁定的「一次性任务投递触发源」模式，不引入新技术。
- **信号监控的 `trade` / `notify_and_trade` 动作是一条断路径，已认定为缺陷**（2026-09-21 认定）。它只做一次 `Order.objects.create()`（`signal_monitor/engine.py`），而全仓没有任何消费者读这些行——`executor.submit_order` 是唯一调用 `place_order` 的地方，且它只被 `trading:orders` 流的消费者触发。产出的行永久停在 `status="pending"`、`exchange_order_id=""`，却计入「活跃订单数」，没有卡单告警也没有清理任务。**入口是可达的**：Web API 的创建/更新序列化器接受 `trade` / `notify_and_trade`（只有 Agent 工具硬编码为 `notify`），所以用户可以在「以为在下单」的情况下得到一个什么都没发生的系统，同时账面被污染。它与「机制把该停的仓位记成已开」是同一个后果，因此 halt 必须处置它——但处置方式是**使路径不可达**，不是拦截它：`_execute_trade` 走 `Order.objects.create`（`engine.py:470`），从不经过 `OrderExecutor`、也从不经过 `pre_trade_check`，「要求 halt 拦它」在实现上**没有落点**。一句无法兑现的拦截要求留在文档里会被当成待办，然后有人去 `_execute_trade` 里加第二个拦截点——那正是 ADR 0001 一路在清除的东西。
- **`request_id` 必须同时作为 `client_order_id` 发往交易所**：模型上写着「幂等键」、适配器也支持 `newClientOrderId`，但没有任何地方填充它——幂等键从未离开本地。接通后交易所侧才真正具备去重能力，崩溃窗口（本地落单成功后、`place_order` 之前进程死掉）才能靠按 `clientOrderId` 反查消解；否则那些行永远停在 `pending` 且 `exchange_order_id=""`，被成交同步的「无交易所 ID 则跳过」永久忽略，且任何未来的下单重试都无法做成幂等的。
- **悬挂 `pending` 行必须按 `clientOrderId` 反查交易所，并区分「确定不存在」与「不知道」**：反查确认交易所无此单 → 标 `failed` + 告警；反查不可达或超时 → 标一档明确的「未知」并告警，**不自动标 `failed`**。直接标 failed 是谎报：若单子其实在交易所，本地记 failed 会让账面与实际分叉，而分叉的账面比空白更危险（与「减仓必须依赖交易所侧只减不增」同一个理由）。**还必须修掉成交同步里「无交易所 ID 则跳过」那句**——否则这个扫描永远覆盖不到这批行；兜底扫描落成 beat 定时任务（当前 beat 里没有任何订单相关任务）。
- **真正打到 testnet 的是 `validate_strategy` 那条链路，不是上面的 `trade` 动作**：`validate_strategy` → Redis List（`strategy:validate:<session>`）→ `LiveStrategyRunner` → `trading:orders` 流 → `OrderExecutor.submit_order` → `BinanceAdapter.place_order`。**但这条链路的入口不止 `validate_strategy`**：`SignalDispatcher` 是全仓唯一发布 `trading:orders` 的地方，而它有**两条**触发路径——live 会话的 K 线管道（`on_kline → _process_kline → _dispatch_targets`）与 `validate_strategy` 触发。所以「唯一真正打到交易所的那条链路」是 `SignalDispatcher` 本身，`validate_strategy` 只是它的两个入口之一；把它记成 `validate_strategy` 链路，会让「会话跑起来之后 K 线一来就下单」这条路径在文档里不存在。沙箱主机名是 **`https://demo-fapi.binance.com`**（USD-M 期货测试网），不是 `demo.binance.com`；是否走沙箱由 `ExchangeAccount.testnet` 决定，API Key/Secret 为空的账户会被整体跳过。判断「订单到底发没发出去」以 `exchange_order_id` 非空为准——它是唯一可靠判据。
- **断路径的处置是收窄入口，不是接通它**：Web API 的**三个**入口去掉 `trade` / `notify_and_trade`，让这类 monitor 无法被创建（Agent 工具本就只写 `notify`）。**数是三个，不是两个**：创建序列化器（`signal_monitor/serializers.py:20-23`）、更新序列化器（同文件 `:36-40`，`patch` 会把既有 notify 型 monitor 改成 trade）、以及 `POST /api/signal-monitor/import/backtest/<id>/`——**最后这个按请求体 `signals` 自由 JSON 取 `action_type`，完全不经过 ChoiceField**，漏掉它就等于留下三者中最宽的一条缝（它还接受任意字符串）。**不选「接通」**——接通会在 halt 落地之前先开出一条下单入口，而它没有 `live_session`、`action_params` 是自由 JSON（`side` 默认 `buy`、`quantity` 默认 0），等于绕过整套设计。收窄的代价为零：线上这类 monitor 计数为 0。历史 `notify` 型行与已存在的订单行不受影响。
- **`LiveSession.mode` 与 `ExchangeAccount.testnet` 必须一致，不一致则拒绝启动**：`paper ⇒ testnet=True`、`live ⇒ testnet=False`。字段名字承诺隔离而实现不隔离，是这份文档里代价最高的一类错误（用户以为在演练、实际在动钱）。**不选接通 `PaperTrader`**——它是内存 dict、无持久化、撮合极简，拿它做验收会得出「真实撮合没问题」的假结论，恰好削弱「验收全部在 paper 上」这条推论；**也不选删掉 `mode`**——用户意图与账户属性是两个维度，合并会丢信息。校验只是把静默错配变成启动失败，不新增能力。现有 6 条会话全部满足该不变式（`paper` + `testnet=True`），无需数据修复。
- **统一架构**：新 halt 状态机是唯一的「停止」抽象，已有的日内亏损/连续下单失败/API 断连/手动触发/日历事件/行情阶段全部降格为触发源。**已有熔断器保持禁用，但接通「手动触发」入口**。
- **已有的日内回撤检查是一个活的静默故障，必须先修**：`daily_snapshots` 表没有任何写入任务，而该检查在「当日已实现亏损」时走「无法获取期初资金数据」分支返回拒绝——任何 paper 会话当天亏一次，之后所有开仓被无声拦掉，且不产生任何告警。**修法是补一个写 `DailySnapshot` 的定时任务，不是放宽这条检查**。

**事件熔断**

- **第一版只做日历型**（可提前确定时间的事件）；突发型明确不在范围内，不假装能防黑天鹅。
- 事件人工维护入库；三档 impact（高/中/低），**仅「高」触发熔断**。高 = 足以在数小时内造成全市场 >5% 波动。
- **作用域必填**（全市场 / 品种列表）——FOMC 与单一代币解锁的熔断范围完全不同。**品种列表必须用与订单同一套 symbol 口径（ccxt 的 `BASE/QUOTE`，如 `SOL/USDT`），并在录入那一步校验：映射不到已知 symbol 的条目直接拒绝入库**，报错里直接给出候选。**口径必须按代码里真实存在的那个写**：订单侧传到风控点的是 `BASE/QUOTE`（`OrderRequest.symbol`），带结算后缀的 `SOL/USDT:USDT` **在全仓不存在**，而 `normalize_binance_symbol`（`backtest/tasks.py:216`）在第一个 `if "/" in s` 处直接返回，**不会处理这种后缀**——若照后者录入，录入端与运行端永不相同，恰好就是本条要防的那种失效。**带结算后缀的符号（`SOL/USDT:USDT`）在录入端直接拒绝，报错写清「本系统暂不支持带结算后缀的符号」**：归一化需要先决定语义（`SOL/USDT` 是现货、`SOL/USDT:USDT` 是 U 本位永续，是两个不同合约），把它们折叠成同一个 key，会让一个现货解锁事件去拦一张永续单——**作用面错了比不拦更危险**；照原样存则回到上面那种「匹配永不成立」的失效，而且这次连报错都没有；拒绝的代价今天是零，因为系统中没有一条路径能产生带后缀的订单。**连带：将来接永续时，符号归一化必须先落地并统一到一处**——现在有四处各自 inline 的 `.replace("/","").upper()`，不统一则品种级熔断会在换到永续那天静默失效；这类「能力扩了、熔断没跟上」与「空事件库上线」是同一种沉默。不这么做会出现最难发现的一种失效：规则在库里、窗口会开启、日志一切正常，就是**匹配不上任何订单**——它与「空事件库上线」同一形态（沉默、且看起来与正常运行完全一样），只是更难发现。**不做模糊匹配**（别名表、大小写归一、前缀匹配）：那会引入「我以为拦住了、其实匹配到了别的东西」这个新失败模式，而作用域是熔断的作用面，**作用面错了比不拦更危险**。把失效点前移到录入时刻，是唯一能让人看见它的位置——运行时无法区分「作用域不匹配」与「本来就不该拦」。
- 默认窗口：事件前 2 小时停 + 事件后 1 小时恢复；允许单事件覆盖，但设全局上下限（15 分钟 ~ 24 小时）。熔断期内若触发既有风控事件，转人工确认才恢复。
- **事件时间沿用项目统一口径（存 UTC、录入按北京时间解释），但确认与日报必须同时回显 UTC 与北京时间两个绝对时刻**：事件天然是美东时间（FOMC 02:00 ET），而窗口只有 ±2h/1h——一次时区误读不是「显示错了」，是**把熔断放在了错误的一天**。口径统一是对的，不该为事件另开一套解释规则；但事件是唯一一个「误读即熔断放错位置」的输入，而「明天凌晨两点」在北京时间口径下本身就歧义（今晚还是明晚）。回显两个绝对时刻让误读在**确认那一步**暴露，而不是在事件当天暴露。
- **同一天多次「高」事件各自独立减仓，允许叠到基准的 25%，但必须告知**：FOMC 窗口结束后两小时又有 CPI，第二次的基准是当下敞口（已减半），于是敞口成为基准的 25%。**不合并、不设每日上限**——总原则是不确定时往保守倒，两个独立的高影响事件挨在一起，敞口更低是对的方向；把它们压成一次等于机制替用户判断「这两件事是一件事」，而这个判断没有依据可援引；设每日上限则会让第二个事件只剩停开新仓，那是「信息到达时不作为」。代价是用户看到一个他没预期过的比例，所以**告警与日报必须写出「本日第 N 次熔断，累计已减至基准的 X%」**——这条告知是硬要求，不是措辞偏好。
- **事件维护入口是 slash 命令，不引入 Django admin**：全仓无 admin，装它等于给一个持有交易所密钥的系统新开一整套登录页、session 与后台面（且会把全部订单、会话、幽灵策略一起挂在后面）。而「提升为『高』必须人工」这条**需要留痕**——走命令天然带着操作人身份，审计链是现成的。**Agent 侧不持有任何事件写权限，只有「建议」这一条路**：它复用同一个判定服务读出候选事件、产出建议文本，**写入一律走 slash 命令**。不按 impact 分级给写权限——中/低虽不触发熔断，却会进入日报第③段与上线确认页的「未来 N 条」，而那两个数字正是「沉默必须能被识别为异常」的载体；「只能写中/低」还会在审计链上把 agent 写的行和人录的行混进同一个列表，而这两者的可信度根本不同。**人工维护的失效模式是覆盖率衰减**：刚开始录几条、三个月后没人管，熔断会安静地永不触发——而它不触发时没有任何信号。**日报第③段兼作提醒**（它本就输出未来 7 天的高影响事件及窗口），另加「超过 14 天无新事件入库」的告警。**不做定期强制确认**——那会把机制变成打卡任务，加速厌倦。
- **事件熔断不可被人工豁免**：`恢复` 命令只作用于行情阶段驱动的停用。事件熔断按定义会自己到期，豁免它等于取消它——在 FOMC 前一次手滑就能放行所有策略。
- **事件改期与取消必须走命令并留痕；窗口内改期不中止已开启的窗口、不改变已发生的动作**：新时间从下一个窗口起生效，变更需通知受影响用户。**不允许「窗口内改期即立即重算」**——那会开出一条明确的后门：在事件前夕按一下「改期」就能中止一个已开启的熔断窗口、避开减仓，与「事件熔断不可被人工豁免」是同一条缝，只是绕了一步；且减仓不可逆，窗口内改期无论如何改变不了已经发生的动作，「立即重算」只会让系统状态与实际持仓对不上。**也不允许「只改期不删除」**——取消的事件必须能从库中移除，否则留下的是「取消的事件永远挂在日历上」的假熔断，它比漏掉更糟：它在不该停的时候停。改期是常态而非异常（FOMC 临时调整、CPI 延迟、解锁推迟），**通知是硬要求**，与「保命档触发时发即时通知」同源。
- **v1 没有角色模型**（所有用户平等，系统级 halt 谁都能解除）。第一版靠可见性而非权限约束：恢复动作记录操作人并广播给全部受影响用户。**「无角色模型」是已知缺口，不假装它不存在。**

**验证与交付**

- **先跑 Shadow**：判定照跑、建议照发、机制自己不施加动作；跑到 ≥3 次高影响事件窗口 + ≥20 个自然日（≈14 个工作日）。**用自然日而非「交易日」**：本机制一律以自然日为口径（见成功标准②），且 20 自然日比 20 交易日短约 30%，这是**刻意放宽**——不为维持原时长而抬到 30 天，因为「≥3 次事件窗口」通常才是实际约束，抬高下限只会让下面那条兜底更常触发。**这 ≥3 个窗口验的不是事件熔断**（Q95 已定 Shadow 只覆盖第①段的判定质量，事件熔断按自己的前置独立上线）——它们验的是判定质量，理由是高影响事件窗口正是资讯最活跃、判定最要紧的日子，Shadow 必须跨过几个这样的日子才算被看过。**必须加下限兜底：若 60 个自然日内未跨过 3 个高影响事件窗口，Shadow 按 ≥20 个自然日到期，并显式记录「事件窗口数不足」**——否则结束时间由事件库的日历决定，而事件库是人工维护、会衰减的（「超过 14 天无新事件入库」的告警就是为这个失效模式设的），一个没人管的库能让 Shadow 无限期挂着，而挂着的样子与正常跑着完全一样。到期时把缺口摆在人面前是唯一能暴露它的方式。**Shadow 从资讯通道可用之后才开始**：判定函数里含资讯（资讯是判定的一等输入），先跑一轮纯量化的 Shadow 要么那轮作废、要么它交出的就是另一个函数的成绩单。**资讯通道的失败模式是「Shadow 不开始」，不是「Shadow 在缺资讯的状态下开始」**。
- **出 Shadow 切到执行态必须人工确认，不自动切换**：到期只代表准入条件可被校验，由系统把校验结果摆出来、人点头才切。理由与「停自动、恢复人工」同源——出 Shadow 是往激进方向的一次动作，而系统唯一能自动做的是往保守方向。成功标准明确不含收益，因此没有任何自动指标能替你回答「该不该上线」。**注意机制自熔断的频率条款要连续 3 个月才评估得出，而 Shadow 期的量级是几十个自然日**，所以它在切换那一刻必然还评估不出来：它是切过去之后继续监控的条款，不是真正的准入门槛，文档不得把它当门槛使用。
- **自熔断退回 Shadow 之后，回到执行态同样要人工确认，且必须显式记录「这次是自熔断后的恢复」，并把根因一并摆出来**（是门槛太松、还是判定确实不行）。不自动切回：那会让一个刚被判定为「不可信」的机制自己宣布自己可信了，正是自熔断要防的东西。也不要求重走一遍完整 Shadow 期：那会让「连续 3 个月频率过高」的代价变成再等几十个自然日，人就会倾向于不修它。根因是必须的——否则恢复动作会退化成按一下按钮。
- **Shadow 期的每日记录独立落库**（每日一条：判定、建议、受影响的策略、实际是否执行=否）。**记录里必须同时留下基础阶段 / 抬升标志 / 生效阶段三个值**，否则「资讯到底改了几次口、改对没改对」在事后无法从数据里回答，而资讯判定恰恰是判定里最需要被审视的一环。Shadow 唯一要回答的三个问题——判定准不准、触发是否太频繁、机制自熔断该不该跳——都要按时间聚合查询，应用日志做不到。它同时是机制自熔断频率条款的数据来源。
- 成功标准三条：① 阶段判定与人工标注的已知历史区间一致率达标 ② **触发频率** < 10% 的**自然日**（分子见下） ③ 每次**判定出的**熔断有完整可读解释。**不含收益**——实盘无法做对照实验。**①的比对基准是纯量化口径**（历史标签只能由量化算法产出，见「行情阶段判定」），资讯抬升的日子单独统计成一项，不能因为它们与量化历史标签不符就算作判定错误。**②的分母是自然日，不是「交易日」**：24/7 市场没有收盘、没有交易日边界，「10% 的交易日」在不同实现里会分别变成 10% 的自然日 / 工作日 / 有判定的日子，而这是准入门槛——分母含糊，门槛本身就不可校验。**也不定义成「有判定的自然日」**：那会在判定失败的日子把分母变小，等于让机制越不稳定、触发率看起来越低，是一个会奖励故障的定义。**②的分子是「任一触发源当日被判定为应当生效的自然日」——判定口径，与是否真的执行无关**——适用性停用、高波动档、事件熔断都算，不叫「停用决策触发频率」：「停用决策」按 Glossary 的定义**不含保命档**，按字面统计会让一个「资讯抬升到高波动、全场天天停开新仓、却从不产生适用性停用」的机制干干净净地通过准入，而它正是最该被 Shadow 拦下的形态。这个指标存在的唯一目的是回答「机制是不是动得太频繁」，而用户不区分「我的策略因为表现差被停」与「此刻谁都不该开仓」——两者在体感上都是「今天又跑不了了」。**分子取「判定」而非「起作用」是必须的，否则 ② 在 Shadow 期恒等于 0**：Shadow 的定义是「机制自己不施加任何动作」，没有任何触发源「起作用」，于是 ② 永远小于 10%、准入自动通过——而 Shadow 要回答的问题之一恰恰写着「触发是否太频繁」，那只能沿**建议**这条轴回答。判定照跑、建议照发，所以 Shadow 期的分子就是「建议触发的日子」；切到执行态后判定与执行合一，这个数自然连续，**不产生口径跳变**。**不选「Shadow 期免检 ②、切过去之后再算」**：那等于把唯一的频率刹车装在上线之后，而 ② 与自熔断的 20% 是同一个指标的两条线（10% 准入、20% 熔断），准入那条不能等到熔断那条的时点才有数。
- **①的比对真值由人工独立标注提供，且必须在算法输出可见之前标完**：人工列一份「已知历史区间」清单（某轮牛市 / 某轮熊市 / 某段箱体 / 某次崩盘），每条只填区间与档位。**标注先于查看算法输出**——否则标注会被算法锚定，一致率变成自己验自己。**只标「已知」的区间**：有争议或事后才看清的日子不入真值，宁少勿滥——一个错的真值会把判对的日子记成判错。**不引用第三方或公开的市场阶段划分**：它们的枚举与我们的四档不同源，比对的是两套定义，一致率会变成一个既不能证明也不能证伪的数字。达标阈值进统一配置面，且**「系统性错向」单列成不达标条件**（把极端行情判成「箱体震荡」哪怕一次也算不达标）：一致率是平均值，一类系统性错误会被大量正确的平静日稀释掉，而它正是这个机制最该被防住的失败模式。
- **保留策略：判定记录 / Shadow 每日记录 / halt 与拦截记录 / 资讯条目原文（含正文）全部永久保留**。**不把资讯正文降级为「只留标题 + 链接」**——那与「可回放」的判据直接冲突：判据是「能重建 LLM 当时看到的输入」，而链接会失效、站点会改稿撤稿，正文一删归因链就断在最需要被审视的一环（资讯已被定为判定输入）。体积不是理由：日频 + 预筛有固定条数上限 + 正文有截断上限，一年量级是几十 MB。**若将来真成为问题，正确的动作是下调正文截断上限，不是删旧记录**——下调只影响新数据，删记录会毁掉旧记录，两者的不可逆性完全不同。**统一保留 12 个月尤其不可取**：判定记录既是「每条停用决策都能回答依据什么」的载体，也是机制自熔断频率条款的数据源，删掉之后那条「连续 3 个月」的窗口永远算不出第二遍。
- 反事实第一版只记录判定/建议/触发频率，字段留位，需要时手动重跑。
- **机制自熔断**：连续 3 个月触发频率 > 20%，或任意一次误停造成损失超阈值 → 自动退回 Shadow 并告警。**它是同一个指标的更松阈值（10% 是准入门槛、20% 是事后熔断线），不是第二套算法**——否则同一个机制会同时「通过了准入」和「触发了自熔断」。**v1 只启用频率条款**：反事实第一版不重算收益，「误停损失」算不出来，字段留位但**不假装生效**——一个算不出来的条件待在配置里只会制造假的安心感。**它不构成切换的执行态准入门槛**：频率条款要连续 3 个月才评估得出，而 Shadow 期只有几十个自然日，切换那一刻它必然无结论——它是切过去之后继续监控的条款。**退回 Shadow 时必须释放全部「由机制施加的」停用**（不动人工退役的 `is_active=False`、不动事件熔断按窗口自然到期的部分）：触发自熔断的两个条件都在说「这个机制的停用决策不可信」，那么继续执行它就更不可信；而 Shadow 的语义必须严格成立——不执行任何市场动作。留着停用的 Shadow 是一边宣布自己可能坏了、一边继续改行为，那它就不可验证，用 Shadow 验证判定质量的整个计划随之失效。这是一次大范围放开，但放开的动作是「回到机制介入之前」，不是新的激进动作；误跑风险由事件熔断独立兜底（它不因机制自熔断而失效）。
- **交付三段**：① 判定 + 切片 + Shadow 记录（零执行）② 事件熔断（不依赖切片结论）③ 行情阶段 gate（依赖切片结论）。若切片数据最终不达标，至少已交付有价值的事件熔断。**资讯通道并入第①段，且它在这段的时长关键路径上**：它是从零建的一整套采集 + 入库 + LLM 判定 + 留痕，不是三段里某一小步，而是一块新增面积。**不单列成第①·5段、也不推迟到 v1 之后**：单列等于先按纯量化跑一轮 Shadow，那是另一个判定函数的成绩单（见「先跑 Shadow」）。
- **Shadow 只覆盖第①段的判定质量，不覆盖事件熔断**：事件熔断触发不依赖判定质量（由人工维护的事件库与确定时间窗驱动），让它等几十个交易日的 Shadow 期没有收益，反而推迟「熔断真能在极端行情里拦住敞口」这件事被检验。**但事件熔断上线前必须满足自己的前置**：只减不增标志先接通，否则那条「自动减仓」不安全；未成交挂单的撤销（Q96）也属于它的前置。**上线确认那一步必须显式回显「未来 14 天高影响事件：N 条」，N=0 时给出明确警告，但不阻止上线**：空库可能只是「下周本来就没有高影响事件」，那是真实状态而非故障；硬性阻止会教人随手录一条假事件绕过它；也不要求先攒够 3 个月历史（历史记录不改善未来的触发）。**但 N=0 必须被看见**——它与日报第③段（未来 7 天高影响事件）和「超过 14 天无新事件入库」的告警是同一件事的三个入口，确认页是其中最及时的那个。**这是「沉默必须能被识别为异常」在准入这一步的落地**：空库上线等于熔断永久空转，而空转的样子与正常运行完全一样。
- **已认定的缺陷是第①段的强制前置，不是机制的一部分**：断路径收窄、`mode` 一致性校验、`request_id` 接通、悬挂单反查、`DailySnapshot` 无写入方与 `_check_drawdown` 静默拒绝。它们的共同点不是「都坏了」，而是**全部静默失效**——不告警、看起来完全正常。而 Shadow 期的全部价值建立在「记录下来的判定与建议可信」上：`mode` 错配能让「paper 验收」实际在动真钱、悬挂单污染订单统计、断路径污染「账面已开仓」。带着它们跑 Shadow，等于用一份已被污染的数据回答「判定准不准」。**它们的量级差一个数量级，但按「一次改造、一个测试面」成组处理，不拆成独立修复项**：`mode` 校验 + 序列化器收窄是十几行，而 `clientOrderId` 接通 + 悬挂单反查是一条新的扫单链路（含 beat 任务、含「未知」这一档）——两者共享同一个测试面（订单能否被交易所侧确认），拆开会让「成交同步里那句『无交易所 ID 则跳过』」被落在两组之间没人改。
- 验收场景全部在 paper 上；**同一个 gate 同时管 paper 与 live**（行为不一致会验证出假结论）。
- **「Shadow 期不执行任何市场动作」指的是机制不施加动作，不是拦住系统里的订单**：Shadow 期判定照跑、建议照发、机制不拦任何订单，用户照常在 paper 上跑策略验收——那段时期恰恰是验收唯一能跑通的窗口（`SignalDispatcher` → `trading:orders` → `OrderExecutor` → `BinanceAdapter` 是全仓唯一真正打到交易所的链路）。**这不与「同一个 gate 同时管 paper 与 live」冲突**，两者说的是不同对象：Shadow = 机制没在动；halt = 此刻谁都不该开新仓。halt 一旦上线（事件熔断先上、行情阶段 gate 后上），它拦 paper 的开仓是**故意**的——验收要求行为一致，否则 paper 上验出来的结论是假的。**连带要求：被 halt 拦下的任何动作必须回显是哪个触发源在拦，且这条回显落在「用户发起动作」的那一刻**——全仓**没有任何**让用户直接下单的接口（Order 只有 GET、无 admin、无管理命令、聊天渠道与 Agent 工具都不建单，`submit_order` 的生产调用者只有 `order_consumer` 一个），用户能发起、会**间接产生**下单动作的只有两个：启动实盘会话与跑验收。它们的订单在 K 线到达之后才产生，**那时没有任何人正在看屏幕**——回显若只在订单被拦的那一刻产生，用户永远看不到它。所以要在「启动会话」的响应里就说「会话已启动，但开仓被〈保命档 / 事件熔断 / 策略停用决策〉拦住」：不阻止启动（存量仓位的止损保护必须照常上线），只把「你启动了但它开不了仓」提前说清楚。ADR 0001 把「停止」的语义从「谁在拦」改成了「谁投的触发源」，用户侧看不到这个源，那条语义就只存在于代码里。**裁剪按「作用域是否命中」决定，不按入口一刀切**：启动/验收的回显**只报命中的层**——不命中就一个字都不说，否则每次启动都附一段与己无关的熔断通告（SOL 解锁窗口里启动一个只跑 BTC 的会话），很快就没有人读了；`query_halt` 则**返回全部生效层**，因为用户主动追问的是「现在系统在拦什么」，裁剪反而会让他怀疑工具在瞒他。这与 Q114 的破例同源：按「这个对象是不是 per-user 为真」决定裁剪，而不是按「哪个入口」决定。
- **日报每天固定一条、必发**（沉默必须能被识别为异常）；由确定性服务直发，落库并暴露给 Agent 只读工具以便追问。五段内容：①**今日新判定**（并明写「自明日 08:00 起生效」）+ 依据的具体数字（分位/斜率，不是「市场波动加大」这类话）②相对昨日的变化，取预告口径（**将**新停用 / **将**解除〈某层〉 / 无变化，**层级**而非笼统的「将恢复」）③未来 7 天的高影响事件及其窗口 ④机制健康（上次判定成功时间、当前是否 Shadow、是否触发过自熔断）⑤**昨日日报的投递结果**。**受众是所有 `is_active` 用户，内容按人裁剪**：判定段（处于什么阶段、依据的具体数字）对所有人相同；停用段只含「你实际受影响的策略」，无活跃会话者看不到该节。判定是有用的公共信息，而「一个你没在跑的策略被停了」纯属噪声——噪声会让人开始忽略日报，那时「必发」就只剩形式。**第①段描述的是「今天刚产出、明日 08:00 才生效」的那条判定，不是此刻生效中的那条**：一天可见期的全部价值就是「异常在生效前被拦下」，而日报是用户唯一能看到待生效结论的地方——只报当前生效态，等于让可见期只存在于代码里。**必须明写生效时刻**，否则「今日判定」这个词就在撒谎（此刻咬人的是另一条）。代价是第①②段读起来是未来时，但这与「停自动、恢复人工」是同一种时间观：机制先把话说出来，再动手。
- **日报由判定任务驱动，在判定任务确定结束（成功或最终失败）之后才发，不固定钟点**：判定有有限重试（3 次、间隔递增），失败补跑可能到 08:10 之后才成功；固定 08:00 发会规律性地发布一份几分钟后就被自己推翻的结论（「日报说判定缺失、随后判定成功」），而告警与真实状态不一致是最伤信任的一类输出。**日报的到达时间因此会浮动，这与「每天固定一条、必发」不冲突**——「固定」指的是每天必有，不是钟点必到。**判定任务最终失败时日报照发**，第①段明写「今日判定缺失，处于保持的上一有效状态」：否则失败日就变成没有日报，而「没有日报」与「日报迟到」在用户眼里是两件不同的事。
- **日报投递失败由独立看门狗发现，不靠日报自报**：第⑤段的自报是**回复式**的（下一条日报里说上一条坏了），它对「连续失败」完全无用，而连续失败恰恰是它最该发现的形态——明天也失败，沉默就自我延续，没有任何东西会响。**当天日报在固定时刻（如 09:00，进统一配置面）仍未成功投递则升级告警**，走已有的任务健康检查通路，不新建告警系统。它与第⑤段构成两条互相独立的路径，与「新任务的两条失败可见性路径」同一形状，这里加的是第三条：**投递本身有没有到**。**看门狗不能靠日报自己告警**——日报自己发不出去时，它也就无法报告自己发不出去。**不用「多投几个渠道互为备份」替代它**：那只是把失败变成「用户收到两条渠道里的一条」，而「某个渠道长期坏了」依然无人知道，也解决不了所有渠道同时失败。
- **人工恢复豁免不穿透保命档**：豁免回答的是「这条策略在这个阶段表现不佳，但我要跑」——一个**关于策略的判断**；保命档回答的是「此刻谁都不该开新仓」——一个**关于当前状态的判断**，与策略无关。让豁免穿透等于给「在 FOMC 前一次手滑就能放行」重开一条缝，只是绕了恢复豁免这一步，而保命档比停用决策更该无豁免，它是最后一道闸。**也不做成「保命档期间暂停豁免计时、窗口过后剩余天数继续」**：那实际是给豁免偷偷续期，且让「豁免还剩几天」变得不可预测。**这条必须写进工具输出**：`query_halt` 与 `query_deactivation_decisions` 返回时要能说出「这条豁免当前不生效，因为保命档在拦」——否则用户会看到一个自己放行过、却又被停的策略，而那与「机制没听见我」在观感上无法区分。
- **保命档触发时发即时通知，不等日报**：事件熔断在**窗口开启与结束各发一条**，开启那条必须含**减仓动作清单与结果**（成功 / 部分 / 失败 / 滑点超限已暂停自动减仓转人工）；高波动档导致的全场停也即时发。理由：减仓发生在 T 时刻而用户在 T+1 才知道，等于把「必发日报」当成延迟一天的告警；而「撤单失败要告警」「减仓失败绝不静默」「滑点超限暂停转人工」都已是硬要求，它们不能等到第二天。**结束那条不能省**——否则窗口结束时的恢复变成静默，那正是「保命档解除」里被明确禁止的事。高波动档不含资金动作，但「你的策略全停了」同属即时。**高波动档的两条都发在「生效那一刻」而不是「判定那一刻」**（即次日 08:00，与 gate 咬人同一瞬间），且**解除也发一条**，与事件熔断对称：即时通知的唯一价值是「动作发生时你立刻知道」，发在判定那一刻等于提前 24 小时预告，而预告已经有归属——日报第①段（它已定为预告口径），两处都发是同一件事说两遍。**解除不能只走日报第②段**：解除是一个状态变化，只在日报里出现就等于允许它静默发生，用户会在某天突然发现策略又跑起来了（与「结束那条不能省」同源）。**两层保命档可以同时生效，所以通知必须按「层」发、且回显还剩几层**：结束那条的措辞不是「已恢复」，而是「**事件熔断层已解除；当前仍被〈高波动档〉拦住，开新仓仍不可用**」，两层都解除时才说恢复；反向同理（阶段回落而事件窗口还开着）。**每次任一层新增或解除时，必须重算并回显全部生效中的触发源**——用户眼里始终是一个集合，不是一条流。理由：ADR 0001 把「停止」的语义从「谁在拦」改成「谁投的触发源」，而**多源并发是这个语义唯一会露馅的场景**——单源时「哪个源」与「还拦不拦」是同一个问题，多源时它们分开了，而把「仍有另一层在拦」说成「已恢复」就是谎报。
- **保命档的即时通知按「作用域是否命中」裁剪，与启动回显是同一条规则**：事件熔断只发给「窗口内有活跃会话、且会话涉及的品种命中作用域」的用户；高波动档（作用域 global）发给全体 `is_active` 用户。**「窗口结束的那条」同样按这条裁剪**——半路建仓的用户可能收不到开启通知却收到结束通知，这是可接受的：结束通知的语义是「你可以动了」，收不到它不会造成任何损失，而**漏掉开启通知才会有损失**，所以不必要求用户集合在窗口内保持稳定。**不选「发给全体」**：全局作用域的事件（FOMC）本来就是全体，而品种级事件发给全体，等于用一条与己无关的熔断通告训练用户忽略通知——那会让真正的全局熔断也被忽略，与日报「按人裁剪」是同一条理由。
- **三个开关各自独立、各自人工确认**：出 Shadow（判定 + 切片 + 自动停用）／事件熔断／行情阶段 gate 分开，各有各的前置与确认时点。**不做单一开关**——「出 Shadow 即全部启用」要么把事件熔断拖回去等切片（已拒），要么让 gate 在切片数据没达标时上线（用没证据的结论停策略）；也**不做「只留 Shadow 总开关、另两个由前置条件自动判定」**——那等于把「该不该上线」交给自动判据，而成功标准明确不含收益，没有任何自动指标能回答这一句。**连带后果**：日报第④段「机制健康」里的「当前是否 Shadow」要扩写成**三个开关的当前状态 + 各自最近一次生效时间**，否则「哪些自动行为是活着的」又只能靠读代码回答。
- **新任务的失败可见性走两条互相独立的路径**：接入已有的任务健康检查（beat 的 `check_task_health`）得到「任务跑了没」，日报第④段的「机制健康」得到「结论新不新」。**不让每个新任务自己写告警**——那会让告警逻辑散落各处，且新任务最容易漏写。这是「沉默必须能被识别为异常」在任务层的落地。
- **新任务的重试按「读安全 / 写危险」区分**：判定任务加有限重试（3 次、间隔递增）——重试只是重读数据重算，无副作用；**自动减仓不加自动重试**（减仓两次是 25% 不是 50%；与它相对的是**子单级**的 ≤2 次重发——同一张单、同一个 `clientOrderId`，交易所侧去重，不产生第二次减仓）；熔断的拦停天然幂等，重试无害。**全仓目前没有任何实际生效的任务重试**（`max_retries` 只是声明，从不触发），所以这条边界必须显式写下——否则将来有人给「每日任务」统一加重试，会正好加在最危险的那个上。
- **Agent 对 halt/gate 无写权限**（只能读）。
- **Agent 侧四个只读工具，边界跟着领域对象走**：`query_regime`（**生效中与待生效两条判定各自成组**：各带三值与依据数字、各带时态标签）、`query_halt`（**全部生效层**，各带作用域与触发源，不裁剪）、`query_deactivation_decisions`（停用决策 + 证据摘要）、`query_events`（未来 **7** 天事件与窗口）。**不做聚合的「机制状态」工具**：四个对象回答四个不同问题，聚合会让「为什么停我」这类追问无法定向到证据摘要，Agent 只能把一大坨状态倒给用户。**这里有一条对既有约定的显式破例**：ToolRegistry 的原则是「user_id 自动过滤，Agent 参数不暴露 user_id」，而本机制是系统级的、判定与 halt 全局唯一——按 user_id 过滤会造出一个不存在的东西（「我的行情阶段」）。所以三个工具**不过滤、返回全局状态**，只有 `query_deactivation_decisions` 按当前用户的活跃会话裁剪（那是唯一 per-user 为真的对象）。破例必须写下来：不写，实现时会默认套用过滤，然后用户看到一个互相矛盾的「系统级判定」。**`query_regime` 必须同时给出「生效中」与「待生效」两条，不能只给一条**：它们回答的是两个不同问题——「为什么我现在被停」（生效态，用户在日报刚说完「明日起下行趋势」之后追问的就是这一条）与「明天我为什么会被停」（待生效那条，而停用决策恰恰由它产生，审计要回答的也是它）。只给一条，就有一个问题永远答不出来，而「同一件事的两个入口给两种说法」正是本机制从头到尾在防的形态（告警与真实状态不一致最伤信任）。**日报第①段与工具输出的措辞必须一致**，否则同一天两个入口对同一件事各说各话。**`query_events` 的查询天数是写死的 7 天，且不由 Agent 传参指定**：它与日报第③段回答的是同一个问题，两处给同一个数才不会出现「日报说有 2 条、追问时工具说有 5 条」。**上线确认页的 14 天刻意不同、也必须不同**：那是上线那一刻的一次性体检（要回答「接下来两周会不会空转」），语义不同所以数值不同，不是同一个数的两个实现。**不给 Agent 传参改天数**：一旦可传，Agent 就会按用户措辞猜一个数（「这周」「近期」「下个月」各滑向不同的 N），而这是个可以写死的常量。

### 内部数据工具设计

- **首批工具范围**：ListBacktestsTool、ListOrdersTool、ListExchangeAccountsTool
- **user_id 过滤**：工具层强制过滤为当前用户，Agent 参数中不暴露 user_id
- **user_id 来源**：复用现有架构，WebSocket 消息 fields.user_id → AgentMessage.user_id → Agent._current_user_id → 自动注入 tc.arguments["user_id"]
- **ExchangeAccount 脱敏**：返回 label/exchange/is_active/testnet，不暴露 api_key_enc/api_secret_enc
- **返回字段精简策略**：
  - BacktestResult：返回基本指标（sharpe/win_rate/drawdown等），不返回大数据 JSONField（equity_curve/drawdown_curve/ohlcv_data/indicator_data），引导用户去前端查看详情
  - Order：返回交易核心字段（symbol/side/status/filled_quantity/realized_pnl），提供 live_session_id 外键引导查看详情
- **ID 参数**：支持模糊匹配开头（startswith），例如 `backtest_id="a1b2"` 匹配所有以 "a1b2" 开头的回测
- **过滤参数**：
  - ListBacktestsTool：backtest_id（startswith）、symbol、strategy_name（icontains）、review_status、limit（默认 20，上限 100）
  - ListOrdersTool：order_id（startswith）、symbol、side、status、limit（默认 50，上限 200）
  - ListExchangeAccountsTool：account_id（startswith）、limit（默认 10，上限 50）
- **返回格式统一**：`{"total": int, "returned": int, "has_more": bool, "results": [...]}`，Agent 可通过 has_more 判断是否引导用户去前端
- **工具 description**：明确过滤能力、返回内容（特别是脱敏）、使用场景（Agent 何时调用）
- **异步数据库查询**：独立辅助函数 + `@sync_to_async` 装饰器 + `close_old_connections()`，复用 signal_monitor.py 的模式
- **工具注册**：在 `apps/agent/tools/__init__.py` 中注册到 ToolRegistry
- **字段类型转换**：UUID → 字符串（完整）、Decimal → float、datetime → ISO 8601、外键 → 关联对象名称（strategy.name）或完整 ID（live_session_id）
- **过滤参数**：支持 symbol/strategy/status（Backtest）、symbol/side/status（Order）
- **排序**：默认按 created_at DESC
- **分页**：首批不分页，返回最近 N 条（N 待定），超出时引导用户去前端查看
- **关联数据**：BacktestTrade/GridSearchJob/LiveSession 不单独提供工具，通过外键 ID 引导用户查看详情

### 新建会话（New Session）

**定义**：用户主动发起的动作，清除当前会话的交互状态，从零开始新对话。

**清除范围：**
- SessionState（退出多轮/暂停/工作流状态）
- conv_history（对话历史归零）

**不清除：**
- L2/L3 长期记忆（跨会话的知识沉淀）
- Frame 状态（交易框架独立于对话生命周期）
